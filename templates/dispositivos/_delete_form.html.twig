<form method="post" action="{{ path('dispositivos_delete', {'idDispositivoHpc': dispositivo.idDispositivoHpc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dispositivo.idDispositivoHpc) }}">
    <button class="btn">Delete</button>
</form>
